import styled from "styled-components"

export default styled.ul`
  position: relative;
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  max-width: 100%;
  padding: 10px;
  gap: 10px;
`
